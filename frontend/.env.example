# Copy to .env.local. Only NEXT_PUBLIC_* vars reach the browser, which is where every
# API call is made from — there is no server-side fetching in this app.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
